/**
 * @param {number} x
 * @param {number} y
 * @returns {number}
 */
export const add = (x: number, y: number) => x + y;

/**
 * @param {number} x
 * @param {number} y
 * @returns {number}
 */
export const sub = (x: number, y: number) => x - y;
